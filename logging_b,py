import logging


#DEBUG-Detailed information, typically of interest only when diagnosing problems.
#INFO-Confirmation that things are working as expected.
#WARNING-An indication that something unexpected happened, or indicative of some problem in the near future (e.g. ‘disk space low’). The software is still working as expected.
#ERROR-Due to a more serious problem, the software has not been able to perform some function.
#CRITICAL-A serious error, indicating that the program itself may be unable to continue running.

logging.basicConfig(level=logging.DEBUG)


def add(a,b):
     ''' Addition of two number is'''
     return a+b

def sub(a,b):
    ''' Subtraction of two number is'''
    return a-b

def mul(a,b):
    ''' multiplicatiion of two number is'''
    return a*b


def div(a,b):
    ''' Division of two number is'''
    return a/b



num1=10
num2=2

logging.warning("heyyyyyyyyyyyy")#this comes bydefault.. it will print

Addition=add(num1,num2)
logging.debug("{} - {} = {}".format(num1,num2,Addition))

Subtraction=sub(num1,num2)
logging.debug("{} + {} = {}".format(num1,num2,Subtraction))

multiplicatiion = mul(num1,num2)
logging.debug("{} + {} = {}".format(num1,num2,multiplicatiion))

Division = div(num1,num2)
logging.debug("{} + {} = {}".format(num1,num2,Division))
